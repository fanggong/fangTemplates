---
# META DATA --------------------------------------------------------------------
title: "标题"
subtitle: "副标题"
author: 
  - "作者甲"
  - "作者乙"
date: "`r Sys.Date()`"
abstract: |
  摘要。
  在这里可以书写公式$E = mc^2$，也可以使用[链接](https://fanggong.pub)。
output:
  bookdown::pdf_document2:
    template: yabai.tex
# ------------------------------------------------------------------------------

# SETTINGS ---------------------------------------------------------------------
documentclass: ctexart

# See followed url for more information about variable for LaTeX
# https://latexref.xyz/Document-class-options.html#Document-class-options
indent: yes
papersize: a4
thanks:
lof: yes
lot: yes
toc: yes
toc-depth: 3
numbersections: yes
secnumdepth: 3
classoption:
  - onecolumn 
  - oneside
  - titlepage
linestretch: 1.5
links-as-notes: yes

# See followed url for more information about geomtry
# https://ctan.math.illinois.edu/macros/latex/contrib/geometry/geometry.pdf
geometry:
  - margin=2.4cm

# See followed url for more information about hyperref
# https://mirrors.rit.edu/CTAN/macros/latex/contrib/hyperref/doc/hyperref-doc.pdf
hyperrefoptions:
  - pageanchor=true
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, warning = FALSE)
library(dplyr)
```

# 文本

## Markdown

**正态分布**最早是棣莫弗在1718年著作的书籍的 Doctrine of Change，及1734年发表的一篇关于二项分布文章中提出的，当二项随机变量的位置参数n很大及形状参数p为1/2时，则所推导出二项分布的近似分布函数就是正态分布。

## R语言脚本

本节内容通过下面的脚本生成
```{r results='asis'}
for (var in names(iris)[1:4]) {
  cat(sprintf("### %s\n\n", var))
  cat(sprintf("在iris数据集中，变量%s的均值为%.2f，标准差为%.2f。\n\n",
              var, mean(iris[,var]), sd(iris[, var])))
}
```

## Latex

<!-- TODO -->

# 公式

## 行内公式

使用**\$**来生成行内公式。

正态分布的概率密度函数是$f(x;\mu,\sigma)={1 \over {\sigma \sqrt{2\pi}}} \exp \left( -{(x-\mu)^2 \over {2\sigma^2}} \right)$。

## 行间公式

使用**\$\$**来生成行间公式。

正态分布的概率密度函数是 
$$
f(x;\mu,\sigma)={1 \over {\sigma \sqrt{2\pi}}} \exp \left( -{(x-\mu)^2 \over {2\sigma^2}} \right)
$$

# 图

## R语言脚本直接生成

在图\@ref(fig:iris)中展示了鸢尾花花萼的长度和宽度的分布情况

```{r iris, echo=FALSE, fig.cap="鸢尾花花萼形状分布"}
library(ggplot2)
dat <- iris
ggplot(dat, aes(Sepal.Length, Sepal.Width)) +
  geom_point(aes(color = Species))
```

## 使用本地图片

使用R语言脚本导入：
```{r eval=FALSE}
knitr::include_graphics("<path-to-graph>")
```

*这里当然也可以使用Markdown语法导入*

# 表格

## Markdown

可以使用Markdown语法绘制表格，但是太落后了，就不写demo了。

## R语言脚本

### `pander`包

更多参数使用细节参考[pander包文档](https://rapporter.github.io/pander/)

```{r}
pander::pander(mtcars[1:5, 1:5], caption = "mtcars数据集的部分展示")
```

### `kableExtra`包

更多参数使用细节参考[kableExtra包文档](https://haozhu233.github.io/kableExtra/awesome_table_in_pdf.pdf)

```{r}
kableExtra::kbl(iris[1:5, 1:5], booktabs = TRUE, caption = "iris数据集的部分展示")
```

## Latex

# 文内引用

## 图的引用



## 表格的引用

## 公式的引用
