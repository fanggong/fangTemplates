
\documentclass[letterpaper]{twentysecondcv}

\usepackage[UTF8]{ctex}

$if(photo)$
\profilepic{$photo$}
$endif$
$if(name)$
\cvname{$name$}
$endif$
$if(job-title)$
\cvjobtitle{$job-title$}
$endif$
$if(birthdate)$
\cvdate{$birthdate$}
$endif$
$if(address)$
\cvaddress{$address$}
$endif$
$if(phone-number)$
\cvnumberphone{$phone-number$}
$endif$
$if(site)$
\cvsite{$site$}
$endif$
$if(mail)$
\cvmail{$mail$}
$endif$

$if(about-me)$
\aboutme{$about-me$}
$endif$

$if(skills)$
\skills{$for(skills)${$skills.skill$/$skills.point$}$sep$,$endfor$}
$endif$

\begin{document}

$body$

\end{document}
